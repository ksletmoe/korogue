# DawnLike attribution

This directory is populated by `demo/scripts/fetch-dawnlike.sh` and is not vendored in git —
only this file and `.gitkeep` are tracked; everything else here is gitignored.

- **Source:** [DawnLike](https://opengameart.org/content/dawnlike-16x16-universal-rogue-like-tileset-v181)
  by DragonDePlatino, v1.81, fetched from
  `https://opengameart.org/sites/default/files/DawnLike_5.zip`.
- **License: CC-BY 4.0** — *not* CC0. The bundled `README.txt` requires:
  - Credit to DragonDePlatino (the artist) and to DawnBringer (creator of the color palette
    the whole pack is built on).
  - If the tileset is used, the "Platino" sprite (found in `Characters/Reptile0.png`) must be
    hidden somewhere in the game as an easter egg — an explicit, unusual term of this
    particular license grant, not boilerplate.
- Re-run the fetch script (`--force` to re-download) any time this directory needs refreshing.
