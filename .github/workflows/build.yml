name: build

on:
  pull_request:
    branches: ['**']
  push:
    paths-ignore:
      - '*.md'
    branches:
      - mainline

jobs:
  build:
    name: Build and Run Tests
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      # korogue consumes kotile as a sibling composite build (settings.gradle.kts:
      # includeBuild("../kotile")); CI must check it out next to korogue. checkout's
      # path must stay inside the workspace, so clone the (public) repo directly.
      - name: Checkout kotile (sibling composite build)
        run: git clone --depth 1 https://github.com/ksletmoe/kotile.git ../kotile
      - uses: actions/setup-java@v4
        with:
          distribution: 'corretto'
          java-version: '21'
          cache: 'gradle'

      - name: Validate Gradle wrapper
        uses: gradle/actions/wrapper-validation@v4

      - name: Build and Run Unit Tests
        run: ./gradlew build --no-daemon

#  publish:
#    name: Publish Snapshot
#    needs: [build]
#    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/mainline'
#    runs-on: ubuntu-latest
#
#    steps:
#      - uses: actions/checkout@v3
#      - uses: actions/setup-java@v3
#        with:
#          distribution: 'corretto'
#          java-version: '11'
#          cache: 'gradle'
#
#      - name: Validate Gradle wrapper
#        uses: gradle/wrapper-validation-action@v1
#
#      - name: Publish to Sonatype
#        env:
#          ORG_GRADLE_PROJECT_sonatypePassword: ${{ secrets.SONATYPE_PASSWORD }}
#          ORG_GRADLE_PROJECT_sonatypeUsername: ${{ secrets.SONATYPE_USERNAME }}
#          ORG_GRADLE_PROJECT_signingKey: ${{ secrets.PGP_SECRET }}
#          ORG_GRADLE_PROJECT_signingPassword: ${{ secrets.PGP_PASSPHRASE }}
#        run: ./gradlew publishToSonatype --no-daemon
